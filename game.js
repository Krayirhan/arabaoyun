const canvas=document.getElementById('game'),ctx=canvas.getContext('2d'),scoreEl=document.getElementById('score'),bestEl=document.getElementById('best'),speedEl=document.getElementById('speed'),startPanel=document.getElementById('start-panel'),gameOver=document.getElementById('game-over'),finalScore=document.getElementById('final-score');let W,H,dpr,playing=false,score=0,best=+localStorage.getItem('gece-surusu-best')||0,lane=1,target=1,flow=0,last=0,spawn=0,traffic=[];bestEl.textContent=String(best).padStart(6,'0');const rand=(a,b)=>a+Math.random()*(b-a);function resize(){dpr=devicePixelRatio||1;W=canvas.clientWidth;H=canvas.clientHeight;canvas.width=W*dpr;canvas.height=H*dpr;ctx.setTransform(dpr,0,0,dpr,0,0)}addEventListener('resize',resize);resize();function roadAt(z){let p=Math.pow(z,.72),center=W/2,width=W*(.12+.58*p);return{y:H*.34+(H*.66)*p,left:center-width/2,right:center+width/2,width}}function carX(l,z=1){let r=roadAt(z);return r.left+r.width*(l+.5)/3}function skyline(){let base=H*.35;ctx.fillStyle='#9aaeb5';ctx.fillRect(0,0,W,H*.37);ctx.fillStyle='#6f858d';ctx.beginPath();ctx.moveTo(0,base);for(let x=0;x<W;x+=48){let h=rand(25,95);ctx.lineTo(x,base-h);ctx.lineTo(x+48,base-h)}ctx.lineTo(W,base);ctx.closePath();ctx.fill();ctx.fillStyle='rgba(230,214,166,.55)';for(let x=20;x<W;x+=51)ctx.fillRect(x,base-rand(35,80),4,5)}function shrub(x,y,s){ctx.fillStyle='#314c42';ctx.beginPath();ctx.arc(x,y,s,0,7);ctx.arc(x+s*.8,y+2,s*.75,0,7);ctx.arc(x-s*.8,y+4,s*.7,0,7);ctx.fill();ctx.fillStyle='#55715a';ctx.fillRect(x-2,y+s*.35,4,s*.9)}function scene(){ctx.clearRect(0,0,W,H);skyline();ctx.fillStyle='#83958e';ctx.fillRect(0,H*.37,W,H*.63);let far=roadAt(0),near=roadAt(1);ctx.fillStyle='#343b3c';ctx.beginPath();ctx.moveTo(far.left,far.y);ctx.lineTo(far.right,far.y);ctx.lineTo(near.right,near.y+5);ctx.lineTo(near.left,near.y+5);ctx.closePath();ctx.fill();ctx.strokeStyle='#d0b85f';ctx.lineWidth=4;for(let i=1;i<3;i++){for(let z=(flow%1)-.15;z<1;z+=.17){let a=roadAt(Math.max(0,z)),b=roadAt(Math.min(1,z+.07)),xa=a.left+a.width*i/3,xb=b.left+b.width*i/3;ctx.beginPath();ctx.moveTo(xa,a.y);ctx.lineTo(xb,b.y);ctx.stroke()}}for(let i=0;i<9;i++){let z=(i/9+flow*.45)%1,r=roadAt(z),side=i%2?-1:1;shrub(side<0?r.left-18-r.width*.03:r.right+18+r.width*.03,r.y+8,5+z*11)}}function drawCar(a){let z=a.z,x=carX(a.l,z),y=roadAt(z).y-20-z*16,w=18+z*24,h=32+z*35;ctx.save();ctx.translate(x,y);ctx.fillStyle=a.color;ctx.beginPath();ctx.roundRect(-w/2,-h/2,w,h,5);ctx.fill();ctx.fillStyle='#26343a';ctx.beginPath();ctx.roundRect(-w*.32,-h*.28,w*.64,h*.3,3);ctx.fill();ctx.fillStyle='#f2e4b1';ctx.fillRect(-w*.32,h*.28,w*.18,3);ctx.fillRect(w*.14,h*.28,w*.18,3);ctx.restore()}function drawPlayer(){let x=carX(lane),y=H-82;ctx.save();ctx.translate(x,y);ctx.fillStyle='#e66d48';ctx.shadowColor='#e66d48';ctx.shadowBlur=10;ctx.beginPath();ctx.roundRect(-22,-38,44,76,8);ctx.fill();ctx.shadowBlur=0;ctx.fillStyle='#22323b';ctx.beginPath();ctx.roundRect(-15,-24,30,24,5);ctx.fill();ctx.fillStyle='#f4d58a';ctx.fillRect(-17,-34,9,4);ctx.fillRect(8,-34,9,4);ctx.restore()}function end(){playing=false;let s=Math.floor(score);finalScore.textContent=s;best=Math.max(best,s);localStorage.setItem('gece-surusu-best',best);bestEl.textContent=String(best).padStart(6,'0');gameOver.classList.remove('hidden')}function start(){playing=true;score=0;flow=0;traffic=[];target=lane=1;startPanel.classList.add('hidden');gameOver.classList.add('hidden');last=performance.now();requestAnimationFrame(loop)}function move(d){if(playing)target=Math.max(0,Math.min(2,target+d))}function loop(t){if(!playing){scene();drawPlayer();return}let dt=Math.min((t-last)/1000,.04);last=t;score+=dt*11;flow+=dt*(.34+score*.0002);lane+=(target-lane)*Math.min(1,dt*12);spawn-=dt;if(spawn<=0){traffic.push({l:Math.floor(Math.random()*3),z:.02,color:['#b9574d','#697f8b','#c28a4d','#7d7098'][Math.floor(Math.random()*4)]});spawn=Math.max(.5,1.15-score/2200)}traffic.forEach(a=>a.z+=dt*(.26+score*.00015));traffic=traffic.filter(a=>a.z<1.08);scene();traffic.forEach(drawCar);drawPlayer();let hit=traffic.some(a=>a.l===Math.round(lane)&&a.z>.83&&a.z<1.02);if(hit)return end();scoreEl.textContent=String(Math.floor(score)).padStart(6,'0');speedEl.textContent=String(Math.min(150,72+Math.floor(score/18)));requestAnimationFrame(loop)}document.getElementById('start-btn').onclick=start;document.getElementById('restart-btn').onclick=start;addEventListener('keydown',e=>{if(e.key==='ArrowLeft'||e.key.toLowerCase()==='a')move(-1);if(e.key==='ArrowRight'||e.key.toLowerCase()==='d')move(1);if(e.key===' '&&!playing)start()});let touch=0;canvas.addEventListener('touchstart',e=>touch=e.touches[0].clientX,{passive:true});canvas.addEventListener('touchend',e=>{let d=e.changedTouches[0].clientX-touch;if(Math.abs(d)>20)move(d>0?1:-1)},{passive:true});scene();drawPlayer();
